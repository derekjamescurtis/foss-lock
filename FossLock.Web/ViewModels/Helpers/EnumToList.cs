using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace FossLock.Web.ViewModels.Helpers
{
    //public class EnumToList<T>
    //    where T : Enum
    //{
    //    public EnumToList()
    //    {
    //    }

    //    public IList<KeyValuePair<string, int>> GetAllValues()
    //    {
    //        var values = Enum.GetValues(typeof(T));
    //        var returnValues = new List<KeyValuePair<string, int>>();

    //        foreach (int v in values)
    //        {
    //            var name = Enum.GetName(typeof(T), v);
    //            returnValues.Add(new KeyValuePair<string, int>(name, v));
    //        }

    //        return returnValues;
    //    }
    //}
}